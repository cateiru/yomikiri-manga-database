# 実装プラン 006: デプロイ・運用

## 概要

Web を Cloudflare Workers へ、バッチをさくらVPS へ配備し、
定期実行と CD（自動デプロイ）を含む運用体制を整える。
CI 自体は [007 ローカル開発環境・CI](./007_ローカル開発環境・CI.md) で設定済みの前提とする。

- 参照: [001_plan.md 8.1 章「実行環境」](../001_plan.md)、[11 章「非機能要件・留意事項」](../001_plan.md)

## 依存関係

- 依存する先行プラン: [003 バッチクローラー](./003_バッチクローラー.md)、
  [005 ジャンル投票](./005_ジャンル投票.md)（全機能実装後の総仕上げ）

## スコープ

| 項目 | 対象 |
| --- | --- |
| Cloudflare Workers へのデプロイ | ✅ 対象 |
| さくらVPS への バッチ配備（Docker + systemd timer） | ✅ 対象 |
| Mackerel によるバッチの死活監視 | ✅ 対象 |
| バッチログの Mackerel 新ログ機能（OTel Collector）への収集 | ✅ 対象 |
| CD（main push での自動デプロイワークフロー） | ✅ 対象 |
| 秘匿情報の管理 | ✅ 対象 |
| CI（lint / typecheck / test / build） | ❌ 対象外（プラン 007 で設定済み） |
| 統計ダッシュボード等の拡張 | ❌ 対象外 |

## 実装タスク

### 1. Cloudflare Workers（apps/web）

- `wrangler.jsonc` を本番用に整備する（プラン 004 で作成済みの設定を拡張）
- `DATABASE_URL` を `wrangler secret put` で登録する
- `opennextjs-cloudflare build && wrangler deploy` によるデプロイスクリプトを
  `package.json` に定義する
- ISR（`revalidate: 300`）が Workers 上で機能することを確認する
- カスタムドメインの割り当て（用意できる場合）

### 2. さくらVPS（apps/batch、Docker）

- Docker / Docker Compose をセットアップする（Node.js を VPS に直接インストールする必要はない）
- バッチは `apps/batch/Dockerfile` でイメージ化する。本番用の `compose.prod.yaml`
  （ローカル開発用の `compose.yaml` とは別ファイル。`db` / `wsproxy` は含まず `batch` のみ定義）を
  `docker compose -f compose.prod.yaml run --rm batch` として都度起動・都度破棄する形で実行する
  - `restart: always` による常駐化は採用しない。バッチは 1 回実行して終了する oneshot 処理のため、
    常駐させると正常終了のたびに即座に再起動され「毎日 00:05・12:05・18:05（JST）」の実行スケジュールと両立しない
- デプロイ方法: VPS 上で `git pull` + `docker compose -f compose.prod.yaml build batch` を行う
  簡易スクリプト（`deploy/bin/deploy.sh`）を用意する
- `DATABASE_URL` は環境ファイル（`/etc/yomikiri/batch.env`、`EnvironmentFile=`、パーミッション 600）で管理し、
  systemd 経由で `docker compose` の環境変数（`${DATABASE_URL}`）として引き渡す
- systemd unit を作成する（`deploy/systemd/`）
  - `yomikiri-batch.service`: `Type=oneshot` で `deploy/bin/run-batch.sh` を実行する。
    このスクリプトが `docker compose run --rm batch` を実行し、成功時のみ
    `/var/lib/yomikiri/last-success` を touch する
  - `yomikiri-batch.timer`: `OnCalendar` で毎日 00:05・12:05・18:05（JST）に起動、`Persistent=true`
- 監視は Mackerel（`mackerel-agent`）で行う（`deploy/mackerel/yomikiri-batch-checks.conf`）
  - `check-file-age` プラグインで `/var/lib/yomikiri/last-success` の鮮度を監視し、
    13 時間（最大実行間隔 12 時間 + マージン）以上更新がなければ警告、16 時間以上でクリティカルとする
    （実行漏れ・ハングの検知）
  - `systemctl is-failed yomikiri-batch.service` をラップしたカスタムチェックで、
    直近の実行失敗（終了コード非 0）を即時検知する
- ログは journald で確認する（`journalctl -u yomikiri-batch`）。バッチコンテナは `--rm` で
  実行のたびに破棄されるため、`docker compose logs` は実行中の一時的な確認にしか使えない
  （`docker compose run` をフォアグラウンドで実行することで、標準出力/標準エラーは
  journald 側にそのまま残る）
- 加えて、Mackerel の新ログ機能（β、OTel Collector 経由）を使い、`yomikiri-batch.service` の
  journald ログを Mackerel 上でも検索・閲覧できるようにする。VPS に MDOT Collector
  （`otelcol-mackerel`、deb パッケージ）をホストインストールし、journald → （JSON パース）→
  Mackerel への転送パイプラインを構成する（設定は `deploy/mackerel/otelcol-mackerel-config.yaml`）。
  既存の `check-file-age` / `systemctl is-failed` によるチェック監視（死活監視）とは別軸の
  仕組みであり、互いに独立して動作する

#### VPS セットアップ手順

1. Docker / Docker Compose をインストールする
2. リポジトリを配置する: `git clone <repo-url> /opt/yomikiri/app`
3. 秘匿情報を配置する
   ```sh
   sudo mkdir -p /etc/yomikiri
   sudo tee /etc/yomikiri/batch.env <<'EOF'
   DATABASE_URL=postgres://...
   EOF
   sudo chmod 600 /etc/yomikiri/batch.env
   ```
4. 状態ディレクトリを用意する: `sudo mkdir -p /var/lib/yomikiri`
5. systemd unit を配置する
   ```sh
   sudo cp /opt/yomikiri/app/deploy/systemd/yomikiri-batch.service \
           /opt/yomikiri/app/deploy/systemd/yomikiri-batch.timer /etc/systemd/system/
   sudo systemctl daemon-reload
   sudo systemctl enable --now yomikiri-batch.timer
   ```
6. Mackerel の check プラグインを配置する（`check-file-age` を含む
   mackerel-check-plugins を別途インストールしておく）。
   `[plugin.checks.*]` は `conf.d/*.conf` の `include` からは読み込まれないため、
   `conf.d/` にはコピーせず `/etc/mackerel-agent/mackerel-agent.conf` の末尾に
   直接追記する。**初回のみ実行すること**（再実行すると内容が重複してしまう）。
   ```sh
   sudo tee -a /etc/mackerel-agent/mackerel-agent.conf < /opt/yomikiri/app/deploy/mackerel/yomikiri-batch-checks.conf
   sudo systemctl restart mackerel-agent
   ```
7. Mackerel の新ログ機能（β）用に Collector（`otelcol-mackerel`）をセットアップする。
   着手前に、Mackerel 組織側でログ機能（β）が有効になっているか（「ログ」画面が存在するか）を
   確認しておくこと。また `yomikiri-batch.service` を一度手動実行し、
   `journalctl -u yomikiri-batch -o json` で `logger.ts` の JSON 行が
   `_SYSTEMD_UNIT=yomikiri-batch.service` としてタグ付けされていることを確認しておく
   （前提が崩れていると、以下の `units` フィルタが何も拾わない）。
   1. インストール
      ```sh
      curl -fsSL https://mackerel.io/file/script/opentelemetry-collector-mackerel/setup-apt.sh \
        | MACKEREL_APIKEY='<Mackerel API キー>' sh
      ```
   2. journal 閲覧権限を付与する
      ```sh
      sudo usermod -aG systemd-journal otelcol-mackerel
      sudo systemctl restart otelcol-mackerel
      ```
   3. パイプライン設定を配置する
      ```sh
      sudo cp /opt/yomikiri/app/deploy/mackerel/otelcol-mackerel-config.yaml \
              /etc/otelcol-mackerel/config.yaml
      ```
   4. `/etc/otelcol-mackerel/otelcol-mackerel.conf` の `OTELCOL_MACKEREL_OPTIONS` を
      以下のように変更する（`MACKEREL_APIKEY` の行は変更しない）
      ```diff
      -OTELCOL_MACKEREL_OPTIONS="--config=mackerel:default"
      +OTELCOL_MACKEREL_OPTIONS="--config=/etc/otelcol-mackerel/config.yaml"
      ```
   5. 設定を検証してから再起動する
      ```sh
      sudo otelcol-mackerel validate --config=/etc/otelcol-mackerel/config.yaml
      sudo systemctl restart otelcol-mackerel
      sudo systemctl status otelcol-mackerel
      ```
   - まずは `deploy/mackerel/otelcol-mackerel-config.yaml` のまま（JSON パースなし）で
     ログが届くこと・`yomikiri-batch.service` 以外が混入していないことを確認してから、
     ファイル内のコメントに従って `transform` processor を有効化し、`level`/`message` 等を
     フィールドとして展開する
   - **既知の制約**: Collector は journal の読み取りカーソルをメモリ上にのみ保持する
     （`file_storage` extension を同梱していないため）。Collector の停止・再起動中に
     実行されたバッチのログはバックフィルされない。死活監視は既存の check 監視で
     カバーされているため実害は小さいが、運用上の既知の制約として留意すること
   - この手順はログ収集を追加するものであり、手順 6 の check プラグイン監視は変更しない
8. 動作確認: `sudo systemctl start yomikiri-batch.service` で手動起動し、
   `journalctl -u yomikiri-batch -f` でログを確認する。あわせて Mackerel の「ログ」画面で
   該当ホストを検索し、`yomikiri-batch.service` 由来のログが `level`/`message` 等の
   フィールド付きで表示されること、他ユニットのログが混入していないことを確認する

#### 更新手順

```sh
cd /opt/yomikiri/app
./deploy/bin/deploy.sh
```

### 3. CD（GitHub Actions）

- main への push で Cloudflare へ自動デプロイするワークフローを
  CI（プラン 007 で設定済み）とは別に追加する
  （`CLOUDFLARE_API_TOKEN` を GitHub Secrets で管理）
- マイグレーションの適用は当面手動運用とし、手順を README に記載する

### 4. 運用ドキュメント

README（またはこのディレクトリ配下）に以下をまとめる。

- ローカル開発のセットアップ手順（Docker Compose・migrate・seed。プラン 007 参照）
- マイグレーションの適用手順
- VPS のバッチ配備・更新手順
- 障害時の確認ポイント（journald ログ、Mackerel の check 監視、Neon ダッシュボード、Workers ログ）

## 完了条件

- 本番 URL でトップページが表示され、投票まで一連の機能が動作する
- VPS 上で timer が毎日 00:05・12:05・18:05（JST）に Docker コンテナとしてバッチを起動し、`oneshots` が更新される
- バッチの失敗・実行漏れが Mackerel で検知できる
- `yomikiri-batch.service` のログが Mackerel の新ログ機能で検索・閲覧できる
- main への push で Cloudflare へ自動デプロイされる
